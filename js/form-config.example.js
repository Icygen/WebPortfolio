/**
 * Local dev: copy this file to form-config.js and add your key from https://web3forms.com
 * (form-config.js is gitignored — live site gets the key from GitHub Secret WEB3FORMS_ACCESS_KEY)
 */
window.PORTFOLIO_FORM = {
  accessKey: "",
};
